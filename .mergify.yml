pull_request_rules:
  - name: Automatic merge on approval
    conditions:
      - "#approved-reviews-by>=1"
      - "#approved-reviews-by=lhb008"
      - base=master
    actions:
      merge:
        method: merge
